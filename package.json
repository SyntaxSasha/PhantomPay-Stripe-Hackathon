{
  "name": "phantompay",
  "private": true,
  "version": "0.1.0",
  "description": "Every subscription gets its own card. The merchant never gets yours.",
  "scripts": {
    "build": "npm --prefix server install && npm --prefix web install && npm --prefix web run build",
    "start": "npm --prefix server start",
    "e2e": "npm --prefix server run e2e",
    "verify": "npm --prefix server run verify"
  },
  "engines": {
    "node": ">=20.18"
  }
}
