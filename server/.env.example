# Copy to .env and fill in. Test mode only — never put a live key here.
STRIPE_SECRET_KEY=sk_test_...

# Optional. Reused across restarts so the demo doesn't pile up cardholders.
PHANTOM_CARDHOLDER_ID=ich_...

PORT=4242
